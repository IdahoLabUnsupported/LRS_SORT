@using LRS.Mvc.Classes
@model LRS.Business.FundingTypeObject
@{
    ViewBag.Title = (Model.FundingTypeId > 0 ? "Edit" : "Add") + " Funding Type";
    Breadcrumb.Add("Dashboard", "/")
        .Next("Lookup Data", Url.Action("Index", "Lookup"))
        .Next("Funding Types", Url.Action("FundingTypes", "Lookup"))
        .Next(ViewBag.Title);
}

<div class="col-md-12">
    <div class="panel panel-primary">
        <div class="panel panel-heading">
            <h3 class="panel-title">@ViewBag.Title</h3>
        </div>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(m => m.FundingTypeId)
            <div class="panel-body form-horizontal">
                <div class="form-group">
                    <label class="control-label col-md-3">Funding Type</label>
                    <div class="col-md-9">
                        @Html.BsTextBoxFor(m => m.FundingType)
                        @Html.ValidationMessageFor(m => m.FundingType)
                    </div>
                </div>
                <div class="form-group">
                    <label class="control-label col-md-3">Description</label>
                    <div class="col-md-9">
                        @Html.BsTextBoxFor(m => m.Description)
                        @Html.ValidationMessageFor(m => m.Description)
                    </div>
                </div>
                <div class="form-group">
                    <label class="control-label col-md-3">Active</label>
                    <div class="col-md-1">
                        @Html.BsCheckBoxFor(m => m.Active)
                        @Html.ValidationMessageFor(m => m.Active)
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-md-offset-3 col-md-9">
                        <button type="submit" class="btn btn-primary">Save</button>
                        <a href="@Url.Action("FundingTypes")" class="btn btn-default">Cancel</a>
                    </div>
                </div>
            </div>}
    </div>
</div>
