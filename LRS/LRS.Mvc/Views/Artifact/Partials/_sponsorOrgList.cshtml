@model List<LRS.Business.MetaDataObject>

<div class="form-group col-md-12">
    @Html.Talon().Grid(Model).Name("sponsors").Columns(c =>
        {
            c.Bound(m => m.MetaDataId).Hidden();
            c.Bound(m => m.MetaDataType).Hidden();
            c.Bound(m => m.Data).Title("Sponsor").Sortable(false);
            c.Buttons(b =>
            {
                b.Button("RemoveSponsor").Icon("times").Type("danger").Title("Remove").ToolTip("Remove Sponsoring Organization");
            }).Width(30).AlignCenter().NoWrap();
        }).Clean()
</div>